

fde_loop = require("execution_loop_wrapper")


-- step function returns new cpu state after executing a single instruction
function STEP(a)

    local cpu = a.cpu.cpu
    local mem = a.memory.memory

    

end
